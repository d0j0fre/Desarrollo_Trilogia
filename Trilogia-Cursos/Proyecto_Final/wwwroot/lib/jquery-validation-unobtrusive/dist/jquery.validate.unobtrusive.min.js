/*
 * Safe local stub for jquery.validate.unobtrusive.
 * Preserves the expected API without intercepting submit events.
 */
(function (window, $) {
    "use strict";

    if (!$) {
        if (window) {
            window.jQueryValidateUnobtrusiveStubLoaded = true;
        }
        return;
    }

    $.validator = $.validator || {};
    $.validator.unobtrusive = $.validator.unobtrusive || {};

    $.validator.unobtrusive.parse = function () {
        return;
    };

    $.validator.unobtrusive.parseElement = function () {
        return;
    };

    $.validator.unobtrusive.validateForm = function () {
        return true;
    };

    window.jQueryValidateUnobtrusiveStubLoaded = true;
})(window, window.jQuery);
