# Rollback 0027

Antes de desactivar `VENTA_MOVIL_CREAR`, `CHAT_USAR` o `METAS_PROPIAS_VER`, desplegar una versión que no los exija. Retirar asignaciones solo con una matriz aprobada; no volver a autorizar por nombres de rol. Registrar la compensación como una migración nueva y conservar el ledger.
